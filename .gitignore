node_modules/
dist/
dist-package/
data/recordings/
# The show's audio: hundreds of megabytes that belong to the client, not here.
data/music/*
# The Compose draft: tied to an audio file that is not in the repo.
data/compose.json
# This install's API key for the artistic direction. Never ours, never shared.
data/direction.json
!data/music/.gitkeep
*.log
.DS_Store
data/patch.reference.json
